_schema-version: 3.3.0
ID: demo
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: demo-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: demo-auth
      - name: demo-destination
  - name: demo
    type: approuter.nodejs
    path: app/
    parameters:
      keep-existing-routes: true
      disk-quota: 256M
      memory: 256M
    requires:
      - name: srv-api
        group: destinations
        properties:
          name: srv-api # must be used in xs-app.json as well
          url: ~{srv-url}
          forwardAuthToken: true
      - name: demo-auth
    provides:
      - name: app-api
        properties:
          app-protocol: ${protocol}
          app-uri: ${default-uri}

resources:
  - name: demo-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: demo-${org}-${space}
        tenant-mode: dedicated
        role-collections:
          - name: 'SO_ManagerRole (demo ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.SO_ManagerRole'
          - name: 'SO_Employee (demo ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.SO_Employee'
        oauth2-configuration:
          redirect-uris:
            - https://*~{app-api/app-uri}/**
    requires:
      - name: app-api
  - name: demo-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
